
import styled from "styled-components/native";

const Task = styled.View `
    margin: 24px 20x 0;
    border-color: #EEEEEE;
    border-width: 1px;
    border-radius: 12px;
    gap: 24px;
`;
const TaskHeader = styled.View `

`;

const TaskDescripition = styled.View `

`;

const TaskFooter = styled.View `

`;

const TaskStatus = styled.View `

`;

const TaskActions = styled.View `

`;
const TaskIcon = styled.Image `
    width:16px;
    Heigth:16px;
`;